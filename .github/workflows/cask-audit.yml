name: cask-audit

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  audit:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v7
      - name: Run Homebrew cask audit
        run: ./scripts/audit-cask.sh
